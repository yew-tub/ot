# AGENTS.md

Project-level instructions for working in this repository.

## Git / GitHub identity

- Repo owner on GitHub: **yew-tub** (`https://github.com/yew-tub/ot.git`).
- Commit authorship is pinned to **yew-tub** (`yew-tub@users.noreply.github.com`).
  Do not change `user.name`/`user.email` in `.git/config`.
- Enforce automatically: before every commit, verify
  `git config user.name` = `yew-tub` and
  `git config user.email` = `yew-tub@users.noreply.github.com`.
  Never commit under any other identity.
- Note: no `yew-tub` gh account is currently logged in on this machine — do not
  `gh`/`git push` to this repo without arranging the correct credentials.